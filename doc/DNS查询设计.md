## 起因
在网友的测试过程中由于vps支持ipv6,在使用本地dns查询的时候优先返回ipv6的地址,
可能会导致netflix断流或一些其他的莫名其妙的症状.遂将dns独立出来做成单独的组件
放至component包下.在每次请求的时候先由组件解析域名而后对dns组件解析的ip进行访
问.

## 设计
dns由主备两个ip组成,于配置文件的`dns_options`中配置,本设计同时请求dns1和dns2
采用优先返回优先使用策略.同时根据配置文件来设置喜好ipv4优先还是ipv6优先.


## 引用
本组件使用`github.com/miekg/dns`提供的dns库封装
